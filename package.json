{
  "name": "impactserver",
  "version": "0.1.0",
  "description": "HTTP API for code impact analysis and review report generation.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "webhook:client": "node src/webhook-client-server.js",
    "test": "node --test",
    "offline:download": "node scripts/download-offline-resources.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
